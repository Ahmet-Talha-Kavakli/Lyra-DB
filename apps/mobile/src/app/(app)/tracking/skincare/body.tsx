import { ComingSoon } from '@/components/coming-soon';

export default function BodyScreen() {
  return (
    <ComingSoon
      title="Body"
      icon="body"
      tint="#7EC8E3"
      message="Movement, posture, and body care tracking — on the way."
    />
  );
}
